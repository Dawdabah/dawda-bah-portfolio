## Packages
framer-motion | Smooth section reveals and interactive animations
react-scroll | Easy smooth scrolling for navigation links

## Notes
- Contact form uses POST /api/contact endpoint
- Profile image is loaded statically from @assets
- Using native CSS scroll-behavior: smooth for base scrolling, but react-scroll helps with active state tracking in the navbar.
